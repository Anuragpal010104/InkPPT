import { BACKEND_URL } from "./ENV";

const API = {
    LOGIN: `${BACKEND_URL}/api/login/`,
    SIGNUP: `${BACKEND_URL}/api/register/`,
    GENERATE_PPT: `${BACKEND_URL}/api/generateppt/`,
};

export default API;